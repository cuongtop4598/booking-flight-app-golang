settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: booking-flight-app-golang
  path: .
  commands: {}
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
